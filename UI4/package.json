{
  "name": "stepby-ui1",
  "version": "2.0.0",
  "private": true,
  "description": "StepBy UI1 — barrier-free map front end (English / Hindi). No build step: the source files are what ships.",
  "scripts": {
    "lint": "eslint .",
    "check:i18n": "node tools/check-i18n.mjs",
    "check": "npm run lint && npm run check:i18n",
    "serve": "python -m http.server 8123",
    "icons": "python tools/build-icons.py"
  },
  "devDependencies": {
    "eslint": "^9.18.0",
    "globals": "^15.14.0"
  }
}
